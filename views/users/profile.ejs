<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
</head>
<body>
    <header>
        <h1>Login!</h1>
    </header>
    <nav>
        <h4><a href="/">Home</a></h4>
        <h4><a href="/profile/<%=index%>/edit">Edit Profile</a></h4>
    </nav>
    <section>
        <h3>Grocery List:</h3>
        <ul>
            <%for(let i = 0; i < groceryItems.length; i++) {%>
            <li>
                <input type="checkbox" name="checkbox">
                <p><%=groceryItems[i].name%></p>
                <p><%=groceryItems[i].section%></p>
                <form action="/items/<%=index%>/<%=i%>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete">
                </form>
            </li>
            <%}%>
        </ul>
        <h3>Add Grocery Item:</h3>
        <form action="/profile/<%=index%>" method="POST">
            <input type="text" name="name">
            <select name="section">
                <%for(let i = 0; i < storeSections.length; i++) {%>
                    <option value="<%=storeSections[i].name%>"><%=storeSections[i].name%></option>
                <%}%>
            </select>
            <input type="submit" value="Add Item">
        </form>
    </section>
    <footer>

    </footer>
</body>
</html>