<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css">
    <title>Login Page</title>
</head>
<body>
    <div class="headernav">
    <header>
        <h1><%=user.name%>'s Grocery List!</h1>
    </header>
    <nav>
        <h4><a href="/">Home</a></h4>
        <h4><a href="/users/profile/<%=user.id%>/edit">Edit Profile</a></h4>
    </nav>
    </div>
    <div class="content">
        <h3>Grocery List:</h3>
        <ul>
            <%for(let i = 0; i < user.Grocery_Items.length; i++) {%>
            <li>
                <input type="checkbox" name="checkbox">
                <p><%=user.Grocery_Items[i].name%></p>
                <p><%=user.Grocery_Items[i].Store_Section.name%></p>
                <form action="/users/items/<%=user.id%>/<%=user.Grocery_Items[i].id%>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete">
                </form>
            </li>
            <%}%>
        </ul>
        <h3>Add Grocery Item:</h3>
        <form action="/users/items/<%=user.id%>" method="POST">
            <input type="text" name="name">
            <select name="sectionId">
                <%for(let i = 0; i < Store_Section.length; i++) {%>
                    <option value="<%=Store_Section[i].id%>"><%=Store_Section[i].name%></option>
                <%}%>
            </select>
            <input type="submit" value="Add Item">
        </form>
    </div>
    <footer>

    </footer>
</body>
</html>